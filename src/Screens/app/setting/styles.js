import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	cont: {
		flex: 1,
		alignContent: 'center',
	},
	contHeader: {
		width: '100%',
		height: '23%',
		// backgroundColor: 'red',
	},
});
export default styles;
